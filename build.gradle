buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.2'
    }
}

ext {
    compileSdkVersion = 21
    buildToolsVersion = "21.1.0"
}

allprojects {
    repositories {
        mavenCentral()
    }
}